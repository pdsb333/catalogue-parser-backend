import LoginForm from "./components/LoginForm";

export default function LoginPage() {
  return (
    <main className="min-h-full bg-bg-alt flex flex-col items-center justify-center p-l pt-32">
      <LoginForm />
    </main>
  );
}
